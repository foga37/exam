🧩Варіант 1: Бібліотека книг
Завдання:
1. Створи клієнтський застосунок на React з такими сторінками:
a. Список книг (із фільтрацією по автору/жанру).
b. Детальна сторінка книги.
c. Форма створення нової книги.
2. Сервер на Express має:
a. REST API для CRUD операцій з книгами та авторами.
b. Валідацію даних (наприклад, назва книги мінімум 2 символи, рік
публікації — число).
c. Підключення до PostgreSQL через pg або knex.
3. Додатково:
a. Реалізуй пошук за назвою книги.
b. Додай можливість прикріпити зображення обкладинки (через
Cloudinary або збереження URL).
c. Виводь помилки серверу у UI.

🧠Варіант 2: ToDo з таймером продуктивності
Завдання:
1. React-застосунок:
a. Головна сторінка зі списком завдань.
b. Можливість створити завдання з назвою, описом і таймером
(наприклад, Pomodoro: 25 хв на задачу).
c. Кнопка "Старт/Стоп" — веде облік часу на виконання завдання.
2. Express-сервер:
a. API для створення/редагування/видалення/отримання завдань.
b. Повертає історію продуктивності (скільки часу витрачено за
день/тиждень).
c. Зберігає таймлоги до PostgreSQL.
3. Додатково:
a. Фільтруй задачі: всі, активні, завершені.
b. Автоматично став таймлог у БД після завершення таймера.

🃏Варіант 3: Меморі гра (Memory Game)
Завдання:
1. React-застосунок:
a. Користувач бачить поле з картками (парні зображення).
b. Після натиску — картка розкривається, якщо 2 однакові — залишаються
відкритими.
c. Кількість ходів і час — відображається на екрані.
2. Express-сервер:
a. Зберігає результати гравців у PostgreSQL: ім’я, кількість ходів, час.
b. Видає топ-10 гравців (лідерборд).
3. Додатково:
a. Додай поле введення імені перед початком гри.
b. Логіка самої гри — тільки на фронтенді.
Правила гри тут: https://www.memozor.com/memory-games/memory-game-rules

🚢Варіант 4: Морський бій (BattleShip)
Завдання:
1. Клієнт на React:
a. Дві сітки: твоя і супротивника.
b. Можна ставити кораблі (1x4, 2x3, 3x2, 4x1).
c. Гра проти бота (рандомна логіка або проста стратегія).
2. Сервер на Express:
a. REST API: старт гри, постріл, збереження статусу гри.
b. Зберігає історію боїв у PostgreSQL.
3. Додатково:
a. Показати статус (хіт/мимо), кількість ходів, перемогу.
b. Вивести історію останніх 10 ігор користувача.
Правила гри тут: https://en.wikipedia.org/wiki/Battleship_(game)

🎬Варіант 5: Кінобаза
Завдання:
1. React SPA:
a. Форма додавання фільму (назва, опис, постер, жанр, рейтинг).
b. Головна сторінка — перелік фільмів з фільтрацією та сортуванням.
c. Детальна сторінка фільму.
2. Express-сервер:
a. CRUD API для фільмів.
b. Окремий ендпоінт для фільтрації по жанру, пошуку по назві, сортуванню
за рейтингом.
c. Дані зберігаються у PostgreSQL.
3. Додатково:
a. Додай рейтингування: користувач може поставити оцінку (1–5).
b. Обчислюй середню оцінку автоматично на сервері.